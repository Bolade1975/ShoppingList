import { en } from './en'

// Single active-language export. When a second language is added, this
// becomes a lookup keyed by a language setting instead of a direct re-export.
export const strings = en
